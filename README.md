# RISC-V-processor-with-M-Extension
A serial multiplication extension with the single-cycle RISC-V ISA that halts the processor when a multiplication instruction arrives and executes the multiplication in 32-cycles, upon completion the output flows to the main processor and in the next cycle (i.e., the 33rd cycle) the processor's normal functionality is resumed.
